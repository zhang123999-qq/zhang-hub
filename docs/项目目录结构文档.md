# 📂 CodeSecHub 项目目录结构

## 项目概览
```
CodeSecHub/
├── backend/                    # Django后端项目
├── frontend/                   # Vue3前端项目
├── deploy/                     # 部署配置
├── docs/                       # 项目文档
├── scripts/                    # 自动化脚本
├── tests/                      # 集成测试
├── docker-compose.yml          # Docker编排配置
├── .env.example               # 环境变量示例
├── .gitignore                 # Git忽略文件
├── README.md                  # 项目说明
└── LICENSE                    # 开源许可证
```

## 详细目录说明

### 1. 后端目录 (backend/)

#### 1.1 项目配置
```
backend/
├── manage.py                  # Django项目管理脚本
├── requirements.txt           # Python生产依赖
├── requirements-dev.txt       # Python开发依赖
├── pytest.ini                # Pytest测试配置
└── setup.cfg                 # 项目配置
```

#### 1.2 Django项目配置
```
backend/backend/           # Django项目配置目录
├── __init__.py
├── settings.py             # Django设置
├── urls.py                 # 项目URL路由
├── asgi.py                 # ASGI配置
├── wsgi.py                 # WSGI配置
└── resources/              # 静态资源配置
    └── __init__.py
```

#### 1.3 应用目录
```
backend/
├── users/                    # 用户认证应用
│   ├── __init__.py
│   ├── admin.py             # 管理后台配置
│   ├── apps.py              # 应用配置
│   ├── models.py            # 数据模型
│   ├── serializers.py       # API序列化器
│   ├── views.py             # 视图函数
│   ├── tests/               # 单元测试
│   │   ├── __init__.py
│   │   ├── test_api.py      # API测试
│   │   └── test_models.py   # 模型测试
│   ├── migrations/          # 数据库迁移
│   │   ├── __init__.py
│   │   └── 0001_initial.py  # 初始迁移
│   └── api/                 # API路由
│       ├── __init__.py
│       └── urls.py
├── blog/                     # 博客应用
│   ├── __init__.py
│   ├── admin.py
│   ├── apps.py
│   ├── models.py
│   ├── serializers.py
│   ├── views.py
│   ├── tests/               # 单元测试
│   │   ├── __init__.py
│   │   └── test_models.py
│   ├── migrations/          # 数据库迁移
│   │   ├── __init__.py
│   │   ├── 0001_initial.py
│   │   └── 0002_initial.py
│   └── api/                 # API路由
│       ├── __init__.py
│       └── urls.py
├── forum/                    # 论坛应用
│   ├── __init__.py
│   ├── admin.py
│   ├── apps.py
│   ├── models.py
│   ├── serializers.py
│   ├── views.py
│   ├── tests/               # 单元测试
│   │   ├── __init__.py
│   │   └── test_models.py
│   ├── migrations/          # 数据库迁移
│   │   ├── __init__.py
│   │   ├── 0001_initial.py
│   │   └── 0002_initial.py
│   └── api/                 # API路由
│       ├── __init__.py
│       └── urls.py
├── resources/                # 资源应用
│   ├── __init__.py
│   ├── admin.py
│   ├── apps.py
│   ├── models.py
│   ├── serializers.py
│   ├── views.py
│   ├── tests/               # 单元测试
│   │   ├── __init__.py
│   │   └── test_models.py
│   └── api/                 # API路由
│       ├── __init__.py
│       └── urls.py
├── admin_panel/              # 自定义管理后台
│   ├── __init__.py
│   ├── admin.py             # 极简管理配置
│   ├── apps.py
│   ├── models.py
│   ├── views.py             # 自定义管理视图
│   ├── urls.py              # 管理路由
│   ├── tests.py             # 测试
│   ├── migrations/          # 数据库迁移
│   │   └── __init__.py
│   └── templates/           # 管理模板
│       └── admin/
│           └── minimal_dashboard.html
└── utils/                    # 工具模块
    └── security.py          # 安全工具
```

#### 1.4 静态文件和媒体文件
```
backend/
├── static/                   # 静态文件（开发）
├── media/                    # 用户上传文件
└── db.sqlite3                # SQLite数据库文件
```

#### 1.5 测试和文档
```
backend/
├── tests/                   # 测试目录
│   ├── __init__.py
│   ├── test_api.py          # API测试
│   └── test_models.py        # 模型测试
└── venv/                    # 虚拟环境
```

### 2. 前端目录 (frontend/)

#### 2.1 项目配置
```
frontend/
├── package.json            # 项目配置和依赖
├── package-lock.json       # 依赖锁文件
├── vite.config.ts         # Vite配置
├── tsconfig.json          # TypeScript配置
├── tsconfig.node.json     # Node.js TypeScript配置
├── index.html             # 入口HTML
├── public/                # 静态资源
│   ├── favicon.ico
│   ├── robots.txt
│   └── manifest.json
└── .env.example          # 环境变量示例
```

#### 2.2 源代码目录
```
frontend/src/
├── main.ts                # 应用入口
├── App.vue                # 根组件
├── shims-vue.d.ts        # Vue类型声明
├── auto-imports.d.ts     # 自动导入声明
├── components.d.ts       # 组件类型声明
│
├── api/                  # API请求封装
│   ├── index.ts         # API统一导出
│   ├── axios.ts         # Axios实例配置
│   ├── auth.ts          # 认证相关API
│   ├── blog.ts          # 博客API
│   ├── forum.ts         # 论坛API
│   ├── resources.ts     # 资源API
│   ├── user.ts          # 用户API
│   ├── upload.ts        # 文件上传API
│   └── types.ts         # API类型定义
│
├── assets/               # 静态资源
│   ├── css/             # 样式文件
│   │   ├── variables.scss  # SCSS变量
│   │   ├── mixins.scss     # SCSS混合
│   │   ├── global.scss     # 全局样式
│   │   └── reset.scss      # 样式重置
│   ├── fonts/           # 字体文件
│   └── images/          # 图片资源
│       ├── logo.png
│       ├── backgrounds/
│       └── icons/
│
├── components/          # 公共组件
│   ├── common/         # 通用组件
│   │   ├── Layout/     # 布局组件
│   │   │   ├── DefaultLayout.vue
│   │   │   ├── AdminLayout.vue
│   │   │   └── AuthLayout.vue
│   │   ├── UI/         # UI基础组件
│   │   │   ├── Button/
│   │   │   ├── Input/
│   │   │   ├── Card/
│   │   │   └── Modal/
│   │   └── Widgets/    # 功能组件
│   │       ├── HeaderNav.vue
│   │       ├── Sidebar.vue
│   │       ├── Footer.vue
│   │       └── BackToTop.vue
│   ├── blog/           # 博客相关组件
│   │   ├── ArticleCard.vue
│   │   ├── ArticleEditor.vue
│   │   ├── CategoryList.vue
│   │   └── TagCloud.vue
│   ├── forum/          # 论坛相关组件
│   │   ├── ThreadCard.vue
│   │   ├── CommentList.vue
│   │   └── ReplyEditor.vue
│   └── resources/      # 资源相关组件
│       ├── ResourceCard.vue
│       └── ResourceUpload.vue
│
├── composables/        # 组合式函数
│   ├── useAuth.ts     # 认证逻辑
│   ├── useBlog.ts     # 博客逻辑
│   ├── useForm.ts     # 表单逻辑
│   ├── usePagination.ts # 分页逻辑
│   ├── useUpload.ts   # 上传逻辑
│   └── useWebSocket.ts # WebSocket逻辑
│
├── router/            # 路由配置
│   ├── index.ts      # 路由主文件
│   ├── routes/       # 路由定义
│   │   ├── blog.ts
│   │   ├── forum.ts
│   │   ├── resources.ts
│   │   └── admin.ts
│   └── guards/       # 路由守卫
│       ├── auth.ts   # 认证守卫
│       └── permission.ts # 权限守卫
│
├── stores/           # Pinia状态管理
│   ├── index.ts     # Store统一导出
│   ├── auth.ts      # 认证状态
│   ├── user.ts      # 用户状态
│   ├── blog.ts      # 博客状态
│   ├── forum.ts     # 论坛状态
│   ├── resources.ts # 资源状态
│   ├── notification.ts # 通知状态
│   └── settings.ts  # 应用设置
│
├── utils/            # 工具函数
│   ├── index.ts     # 工具统一导出
│   ├── format.ts    # 格式化工具
│   ├── storage.ts   # 存储工具
│   ├── validation.ts # 验证工具
│   └── security.ts  # 安全工具
│
├── views/            # 页面组件
│   ├── auth/         # 认证相关页面
│   │   ├── Login.vue
│   │   ├── Register.vue
│   │   └── ForgotPassword.vue
│   ├── blog/         # 博客相关页面
│   │   ├── Home.vue
│   │   ├── ArticleList.vue
│   │   ├── ArticleDetail.vue
│   │   ├── ArticleCreate.vue
│   │   └── ArticleEdit.vue
│   ├── forum/        # 论坛相关页面
│   │   ├── ForumHome.vue
│   │   ├── ThreadList.vue
│   │   ├── ThreadDetail.vue
│   │   └── ThreadCreate.vue
│   ├── resources/    # 资源相关页面
│   │   ├── ResourceList.vue
│   │   ├── ResourceDetail.vue
│   │   └── ResourceUpload.vue
│   ├── user/         # 用户相关页面
│   │   ├── Profile.vue
│   │   ├── Settings.vue
│   │   └── Dashboard.vue
│   └── admin/        # 管理相关页面
│       ├── AdminDashboard.vue
│       ├── UserManagement.vue
│       ├── ContentManagement.vue
│       └── SystemSettings.vue
│
└── plugins/          # 插件
    ├── element-plus.ts # Element Plus配置
    ├── axios.ts        # Axios插件
    └── dayjs.ts        # 日期时间插件
```

### 3. 部署目录 (deploy/)

```
deploy/
├── nginx/              # Nginx配置
│   ├── nginx.conf      # 主配置文件
│   └── sites-available/ # 站点配置
├── docker/             # Docker配置
│   ├── Dockerfile.backend # 后端Dockerfile
│   ├── Dockerfile.frontend # 前端Dockerfile
│   └── docker-compose.prod.yml # 生产环境Docker Compose
├── scripts/            # 部署脚本
│   ├── deploy.sh       # 部署脚本
│   ├── rollback.sh     # 回滚脚本
│   └── healthcheck.sh  # 健康检查脚本
└── config/             # 配置文件
    ├── env.prod        # 生产环境变量
    └── env.staging     #  staging环境变量
```

### 4. 文档目录 (docs/)

```
docs/
├── 系统架构文档.md        # 系统架构文档
├── API文档.yaml          # API文档（OpenAPI格式）
├── 项目目录结构文档.md      # 项目目录结构文档
├── 开发指南.md           # 开发指南
├── 部署指南.md           # 部署指南
└── API_DOCS.md           # API文档（Markdown格式）
```

### 5. 脚本目录 (scripts/)

```
scripts/
├── setup.sh             # 项目初始化脚本
├── build.sh             # 构建脚本
├── test.sh              # 测试脚本
├── lint.sh              # 代码检查脚本
└── generate_test_data.py # 生成测试数据脚本
```

### 6. 测试目录 (tests/)

```
tests/
├── __init__.py
├── test_integration.py   # 集成测试
├── test_end_to_end.py    # 端到端测试
└── fixtures/             # 测试数据
    ├── users.json
    ├── articles.json
    └── threads.json
```

## 配置文件说明

### 1. 环境变量示例 (.env.example)
```
# Django设置
SECRET_KEY=your-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# 数据库设置
DATABASE_URL=sqlite:///db.sqlite3

# Redis设置
REDIS_URL=redis://localhost:6379/0

# 邮箱设置
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-email-password

# JWT设置
JWT_SECRET_KEY=your-jwt-secret-key
JWT_ACCESS_TOKEN_LIFETIME=24:00:00
JWT_REFRESH_TOKEN_LIFETIME=7:00:00:00

# 安全设置
CSRF_COOKIE_SECURE=False
SESSION_COOKIE_SECURE=False
SECURE_SSL_REDIRECT=False
```

### 2. Git忽略文件 (.gitignore)
```
# 虚拟环境
venv/
env/

# 编译文件
__pycache__/
*.pyc
*.pyo
*.pyd

# 数据库文件
db.sqlite3
*.db

# 媒体文件
media/

# 静态文件收集目录
staticfiles/

# 环境变量
.env
.env.local
.env.*.local

# 日志文件
*.log
logs/

# IDE文件
.idea/
.vscode/
*.swp
*.swo
*~

# 操作系统文件
.DS_Store
Thumbs.db

# 构建文件
dist/
build/
*.egg-info/

# 测试覆盖率
htmlcov/
.coverage
.coverage.*
.pytest_cache/
```

### 3. Docker Compose配置 (docker-compose.yml)
```yaml
version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - DEBUG=True
      - SECRET_KEY=your-secret-key
      - DATABASE_URL=sqlite:///db.sqlite3
    command: python manage.py runserver 0.0.0.0:8000

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - VITE_API_URL=http://localhost:8000/api
    command: npm run dev

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  celery:
    build:
      context: ./backend
      dockerfile: Dockerfile
    volumes:
      - ./backend:/app
    environment:
      - DEBUG=True
      - SECRET_KEY=your-secret-key
      - DATABASE_URL=sqlite:///db.sqlite3
    command: celery -A backend worker --loglevel=info
    depends_on:
      - redis
      - backend
```

## 项目启动和开发流程

### 1. 后端开发流程
1. 进入后端目录：`cd backend`
2. 创建虚拟环境：`python -m venv venv`
3. 激活虚拟环境：`venv\Scripts\activate` (Windows) 或 `source venv/bin/activate` (Linux/Mac)
4. 安装依赖：`pip install -r requirements.txt`
5. 运行迁移：`python manage.py migrate`
6. 创建超级用户：`python manage.py createsuperuser`
7. 启动开发服务器：`python manage.py runserver`

### 2. 前端开发流程
1. 进入前端目录：`cd frontend`
2. 安装依赖：`npm install`
3. 启动开发服务器：`npm run dev`

### 3. Docker开发流程
1. 在项目根目录运行：`docker-compose up --build`
2. 访问前端：`http://localhost:3000`
3. 访问后端API：`http://localhost:8000/api`
4. 访问管理后台：`http://localhost:8000/admin`

## 项目技术栈

### 后端技术栈
- **Web框架**: Django 4.2
- **REST API**: Django REST Framework 3.14
- **数据库**: SQLite (开发), MySQL (生产)
- **缓存**: Redis 7
- **消息队列**: Celery + Redis
- **认证**: JWT
- **部署**: Docker, Nginx

### 前端技术栈
- **框架**: Vue 3.3.4 + TypeScript
- **构建工具**: Vite 4
- **状态管理**: Pinia 2
- **路由**: Vue Router 4
- **UI框架**: Element Plus
- **HTTP客户端**: Axios
- **CSS预处理器**: SCSS
- **代码规范**: ESLint + Prettier

## 项目特性

1. **用户认证系统**：支持注册、登录、JWT认证
2. **博客系统**：支持文章的创建、编辑、删除、分类、标签
3. **论坛系统**：支持帖子的创建、回复、点赞
4. **资源系统**：支持资源的上传、下载、分类
5. **管理后台**：自定义的极简管理后台
6. **安全特性**：密码哈希、CSRF防护、XSS防护
7. **性能优化**：Redis缓存、异步任务处理
8. **开发工具**：完整的测试套件、代码规范检查

## 项目文档

- **系统架构文档**: `docs/系统架构文档.md`
- **API文档**: `docs/API文档.yaml` 和 `docs/API_DOCS.md`
- **项目目录结构文档**: `docs/项目目录结构文档.md`
- **开发指南**: `docs/开发指南.md`
- **部署指南**: `docs/部署指南.md`